# payment-zarinpal

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build payment-zarinpal` to build the library.

## Running unit tests

Run `nx test payment-zarinpal` to execute the unit tests via
[Jest](https://jestjs.io).
