# ai-recommendations

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build ai-recommendations` to build the library.

## Running unit tests

Run `nx test ai-recommendations` to execute the unit tests via
[Jest](https://jestjs.io).
